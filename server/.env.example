# MongoDB Atlas connection string (replace <username>, <password>, <cluster-url>)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>.mongodb.net/forum_db?retryWrites=true&w=majority

# JWT secret for signing authentication tokens
JWT_SECRET=your_jwt_secret_here

# (Optional) Port for the Express server
PORT=5000
